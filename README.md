# Azure Data Factory Typed Sales Pipeline
ADF Pipeline that loads .csv into Azure SQL tables using appropriate data types.

## Project Overview

This project demonstrates an ADF pipeline that moves CSV sales data into Azure SQL with specific data types.


